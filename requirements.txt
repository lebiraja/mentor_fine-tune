# ClarityMentor v3 backend — GPU-accelerated STT, CPU TTS
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.136.3
uvicorn[standard]==0.49.0
websockets==16.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.1
aiosqlite==0.22.1
neo4j>=5.28.0,<7.0.0

# STT (GPU via CTranslate2)
faster-whisper>=1.1.0

# TTS — English (ONNX, CPU)
onnxruntime==1.26.0
kokoro-onnx==0.5.0

# TTS — Tamil (Piper, ONNX, CPU)
piper-tts>=1.2.0

numpy==2.4.6
pyyaml==6.0.3

# Emotion Detection
torch==2.5.1
transformers==4.47.1
hsemotion-onnx==0.3.1
Pillow==11.3.0

# Tests (kept in image so `docker compose run --rm backend pytest` works)
pytest==9.0.3
pytest-asyncio==1.4.0
